<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Replace Or Suggest</title>
</head>
<body>
        <div class="grid grid-cols-6">
            <div class="col-start-3 col-span-2">
                <form action="/override" method="post">
                    <input type="hidden" name="title" value="<%= title %>">
                    <input type="hidden" name="start" value="<%= start %>">
                    <input type="hidden" name="end" value="<%= end %>">
                    <p>Already appointment exists in the given time. Do you want to delete the old appointment and replace with new appointment?</p>
                    <button type="Submit">Replace</button>
                    <a href="/appointment">Cancel</a>
                </form>
            </div>
        </div>
</body>
</html>